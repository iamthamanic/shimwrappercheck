/** Resolve first existing backend directory from SHIM_BACKEND_PATH_PATTERNS (comma-separated). */
export declare function resolveBackendDir(
  projectRoot: string,
  patterns?: string,
): string | null;
//# sourceMappingURL=resolve-backend-dir.d.ts.map
