{
  "description": "ESLint snippet for cyclomatic complexity (max 10 per function). Merge into your project's eslint config.",
  "overrides": [
    {
      "files": ["src/**/*.js", "src/**/*.ts", "src/**/*.jsx", "src/**/*.tsx"],
      "plugins": ["complexity"],
      "rules": {
        "complexity": ["error", { "max": 10 }]
      }
    }
  ]
}
